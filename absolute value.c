
#include <stdio.h>

int main()
{
    int n;
    printf("enter any one number:");
    scanf("%d",&n);
    if(n>=0)
    {
        printf("the given number is positive:");
    }
    else
    {
        printf("the given number is negitive:");
    }

    return 0;
}
