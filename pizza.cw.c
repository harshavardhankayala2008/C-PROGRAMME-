#include<stdio.h>
int main()
{
    int pizza,slices;
    printf("enter the no.of pizzas");
    scanf("%d",&pizza);
    slices=8*pizza;
    printf("the no of pizzas=%d\nthe number of slices %d",pizza,slices);
    return 0;
}
